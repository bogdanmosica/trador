# Core web framework
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.10.0

# Essential data analysis (required for statistical arbitrage)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Configuration support now uses JSON (built-in)

# Optional: Machine learning (only install if you need advanced features)
# scikit-learn>=1.3.0

# Optional: Testing framework (only install if you want to run tests)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0

# Optional: Development tools (only install if you're developing)
# black>=23.0.0
# isort>=5.12.0
# flake8>=6.0.0
# mypy>=1.5.0